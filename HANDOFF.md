# 开发接手指南

这份文档是新开发的第一入口。它说明当前产品、两个仓库如何配合、从哪里开始读代码，以及哪些方向还没有完成。接口细节仍以后端 `API.md` 和实际代码为准。

## 1. 接手快照

- 审计日期：2026-08-18。
- 前端 `main`：`edab2c6bbd7dbace7c91e88547649fcc4039a93e`，审计时与 `origin/main` 一致。
- 后端 `main`：`2ba5fa90706f5ec3008428d7dcd3c48a0050abdc`，审计时与 `origin/main` 一致。
- 两个仓库独立提交、成对发布。Git 的 `main` 只能说明代码基线；线上实际版本必须从 release manifest 和后端 `/health` 读取。
- 本次交接整理只改文档，没有改业务代码、服务器、模型、用户素材或线上服务。

配套后端：<https://github.com/huhu771912249-wq/guanxi-canvas-backend>

## 2. 项目在做什么

这是一个 AI 广告素材创作工作台，既提供单功能表单，也提供可自由拖拽、连线和保存的 Vue Flow 画板。当前主要链路是：

```text
文生图（Z-Image / Krea 2 Turbo）
  → H3 首帧生视频
  → 视频转 GIF
  → 静态文字 / 图片水印 / 圆角
  → 素材导出
```

各节点是独立积木，用户可以替换、删除、移动和重新连线，并不是写死的一条流水线。现有产品还包括视频尺寸处理、小说成片、素材裂变、DSP 素材、测试素材、最近生成、最近项目和统一任务中心。

前端负责界面、输入校验、画板编排、状态展示和下载入口；后端负责鉴权、项目持久化、任务状态、模型调用、GPU 队列、FFmpeg 处理和素材存储。

## 3. 用户入口与数据去向

| 入口 | 作用 | 主要持久化 |
| --- | --- | --- |
| 首页 | 对话意图、快捷工具、工作流、最近生成、最近项目 | 后端项目与素材接口 |
| 画板 | 节点编排、生成、继续处理 | `/v1/projects` |
| 视频创作中心 | 快速创作、小说、素材等表单 | 对应任务接口 |
| GIF 编辑器 | GIF/视频加静态文字、图片水印、尺寸、帧率、颜色、循环和圆角 | resize job + 画板节点状态 |
| 任务中心 | 汇总视频、裂变、小说、DSP、尺寸/文字任务 | `/v1/tasks` |
| 最近生成 | 只看生成结果，不混入用户原始上传 | `/v1/assets/recent` |

浏览器 `localStorage` 只保存当前项目 ID、同步时间和界面状态等轻量数据。节点、连线、参数和任务 ID 保存到后端项目 JSON；图片、视频、GIF 和音频二进制只保存为后端素材 URL，不允许把 `data:` 或 `blob:` 写入项目。

## 4. 30 分钟阅读顺序

1. [README](./README.md)：入口和仓库边界。
2. [架构](./ARCHITECTURE.md)：前后端边界、缓存和数据流。
3. [本地开发](./DEVELOPMENT.md)：启动、测试和联调。
4. [前端 API 契约](./API.md)：前端当前消费的接口。
5. `src/router/index.js`：页面入口和登录保护。
6. `src/views/Home.vue`、`src/views/Canvas.vue`：首页与画板主流程。
7. `src/components/nodes/`：各业务节点。
8. `src/api/`、`src/stores/`、`src/config/`：接口、项目状态和工作流/模型配置。
9. [视频广告素材流程](./docs/video-material-workflow.md) 与 [产品后续方向](./docs/product-direction.md)。

## 5. 本地跑起来

先启动后端并确认 `http://127.0.0.1:8788/health`，再启动前端：

```bash
corepack enable
pnpm install --frozen-lockfile
pnpm dev
```

访问 `http://127.0.0.1:5173/huobao-canvas/login`。Vite 会把 `/auth`、`/v1` 和 `/public-assets` 代理到本机 8788 端口。测试账号通过本地环境变量配置，不要写入仓库。

提交前只需运行一次完整门禁：

```bash
pnpm run ci
```

需要真实模型、GPU 或 FFmpeg 的链路应另做集成验收，并明确区分“代码测试通过”“服务已部署”和“真实成品验收通过”。

## 6. 开发与发布方式

1. 同步最新 `main`，从短期 `codex/`、`feature/`、`fix/` 或 `docs/` 分支开发。
2. 一次提交只解决一个业务问题；业务行为变化先补失败回归测试。
3. 接口变化同步修改前后端 `API.md`。
4. 推 PR，CI 和 Review 通过后 squash 合并。
5. 部署时使用前后端两个精确 SHA 生成不可变 release manifest。
6. 部署后核对 `/health`、登录、关键任务和真实成品；异常按 [回滚文档](./ROLLBACK.md) 成对回退。

仓库不保存生产密码、Cookie、Token、模型、任务状态、用户素材或生成媒体。接手人需要向项目负责人单独获取服务器权限和受控环境配置。

## 7. 当前未完成方向

- 账号体系：等待新的 Telegram 鉴权方案；账号级素材、项目、任务和工作流隔离尚未进入当前实现。
- 专业 GIF/NLE：多视频拼接、基础转场、多轨时间轴、字体上传和素材模板仍是后续阶段。
- 首页智能入口当前只在已验证的快捷工具和工作流中推荐，不会任意生成未知节点。
- H3 节点展开态在直接访问画板 URL 加载时会自行上移（`moveNodeByScreenOffset` 在 mount 时触发），header 可能跑出视口上边缘导致抓不住。行为如何收敛（钳制在视口内，还是不在 mount 时移动）尚未定。

产品阶段和已确认边界见 [产品后续方向](./docs/product-direction.md)。新增范围前先与负责人确认，避免把未实现的动画、转场或模型能力展示成可用功能。

## 8. 交接验收清单

- 能分别拉取两个仓库并确认当前分支和 SHA。
- 能启动后端、读取 `/health`，再登录本地前端。
- 能创建和刷新一个画板项目，节点、连线和任务 ID 不丢失。
- 能分清“用户上传素材”“最近生成结果”“最近项目”和“任务中心”。
- 能从文生图或已有素材进入 H3/GIF/文字水印/导出链路。
- 知道模型和云服务缺失时应显示真实失败，不做静默假成功。
- 知道发布和回滚必须使用前后端成对 SHA，不直接覆盖线上 `current`。

