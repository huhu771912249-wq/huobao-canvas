# 回滚

## 原则

- 只回滚到 manifest 完整、曾验证通过的不可变 release。
- 前后端按 manifest 成对回退。
- 不删除当前失败 release，保留取证；用户数据和任务状态不进入 Git。

## 流程

1. 记录当前 `current` 指向和 `/health` 双仓 SHA。
2. 核对目标 release manifest 与产物校验值。
3. 确认目标前端与后端接口兼容。
4. 原子切换 `current` 软链接。
5. 验证静态页面、登录、关键只读接口和 `/health`。
6. 若验证失败，立即切回原 release 并记录失败证据。

回滚不修改数据库结构；涉及数据迁移时必须先采用后端仓库的备份与恢复流程。
